<%= form_with(model: @chatter, local: true) do |form| %>
    <div class="chatter_content">
        <%= form.label :content %>
        <%= form.text_field :content %>
    </div>
    <%= form.submit %>
    
<% end %>